(ns pi.components.notify
  (:require [om.core         :as om
                             :include-macros true]
            [om.dom          :as dom
                             :include-macros true]
            ))

(defn notification-view [f cursor m]
  (om/build nil nil))
